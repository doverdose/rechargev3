{
	"name": "recharge-health",
	"private": true,
	"dependencies": {
  
	},
	"devDependencies": {
		"supertest": "latest",
		"should": "latest",
		"mocha": "latest",
		"grunt": "~0.4",
		"grunt-contrib-copy": "~0.4",
		"grunt-contrib-concat": "~0.3",
		"grunt-contrib-uglify": "~0.2",
		"grunt-contrib-jshint": "~0.7",
		"grunt-contrib-cssmin": "~0.7",
		"grunt-express-server": "~0.4",
		"grunt-contrib-clean": "~0.5",
		"grunt-contrib-htmlmin": "~0.1",
		"grunt-contrib-imagemin": "~0.4",
		"grunt-contrib-watch": "~0.5",
		"grunt-usemin": "~2",
		"matchdep": "~0.3",
    "event-stream": "*",
		"grunt-sass": "~0.11",
    "jquery": "~2",
		"lodash" : "~1.0.1",
		"glob" : "~3.1.21",
    "JSONStream" : "*",
		"minimatch" : "*",
		"grunt-lib-contrib" : "*",
		"underscore" : "*",
		"uglify-js" : "*"
	},
	"scripts": {
		"dev": "npm install && bower install && grunt server",
		"build": "npm install && bower install && grunt",
		"test": "NODE_ENV=test ./node_modules/.bin/mocha --reporter spec test/test-*.js",
		"production-test": "npm run-script build && npm test && NODE_ENV=production node web.js",
		"start": "node web.js",
    "nidebug": "node-inspector --web-port 8888",
    "debug": "node debug web.js"
	}
}
